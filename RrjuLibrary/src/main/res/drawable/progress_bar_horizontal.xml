<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@android:id/background"
        android:drawable="@android:color/transparent" />
    <!-- <item android:id="@android:id/secondaryProgress"> -->
    <!-- <clip> -->
    <!-- <shape> -->
    <!-- <corners android:radius="0dip" /> -->


    <!-- <gradient -->
    <!-- android:angle="270" -->
    <!-- android:centerColor="@color/webview_with_progress_color_n" -->
    <!-- android:endColor="@color/webview_with_progress_color_n" -->
    <!-- android:startColor="@color/webview_with_progress_color_n" /> -->
    <!-- </shape> -->
    <!-- </clip> -->
    <!-- </item> -->

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:angle="270"
                    android:centerColor="@color/webview_with_progress_color_n"
                    android:centerY="0.75"
                    android:endColor="@color/webview_with_progress_color_p"
                    android:startColor="@color/webview_with_progress_color_p" />
            </shape>
        </clip>
    </item>

</layer-list>