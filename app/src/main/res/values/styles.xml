<resources>
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/rrj_theme_color</item>
        <item name="colorPrimaryDark">@color/rrj_theme_color</item>
        <item name="colorAccent">@color/rrj_theme_color</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="actionBarSize">48dp</item>

        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">false</item>
        <!--<item name="checkboxStyle">@style/AppTheme.checkboxStyle</item>-->
    </style>
</resources>
